# 기획안: 로그인 페이지

## 1. 개요

### 목적
현재 Agent Pipeline Dashboard는 인증 없이 누구나 접근 가능하다. 로그인 페이지를 구현하여 대시보드 접근을 인증된 사용자로 제한하고, 파이프라인 실행/승인 등 핵심 기능을 보호한다.

### 핵심 가치
- **접근 통제**: 인증된 사용자만 대시보드와 파이프라인을 조작할 수 있다
- **보안 기반 확보**: 향후 사용자별 권한 관리, 감사 로그 등의 기반이 된다
- **계정 안전성**: 비밀번호 해싱, JWT 토큰 기반의 안전한 인증 흐름을 제공한다

### 작업 범위

**포함:**
- 로그인 페이지 UI (이메일 + 비밀번호 폼)
- 인증 API (로그인 / 로그아웃 / 토큰 검증)
- JWT 기반 인증 토큰 관리
- 사용자 테이블 (SQLite DB 도입)
- 초기 관리자 계정 시딩 (seed 스크립트)
- 미인증 사용자 리다이렉트 (라우트 가드)
- 기존 pipeline API에 인증 미들웨어 적용

**미포함:**
- 회원가입 페이지 (초기에는 시딩으로 관리자 계정만 생성)
- 비밀번호 찾기/재설정
- 소셜 로그인 (Google, GitHub 등)
- 사용자 관리 페이지 (CRUD)
- 역할 기반 권한 관리 (RBAC)

---

## 2. 유저 스토리

| ID | 사용자 유형 | 스토리 | 우선순위 |
|----|-----------|--------|---------|
| US-01 | 관리자 | 관리자로서 이메일과 비밀번호로 로그인하고 싶다, 대시보드에 접근하기 위해 | P0 |
| US-02 | 관리자 | 관리자로서 로그아웃하고 싶다, 공용 PC에서 세션을 종료하기 위해 | P0 |
| US-03 | 미인증 사용자 | 미인증 사용자로서 대시보드 접근 시 로그인 페이지로 이동되길 원한다, 보안을 유지하기 위해 | P0 |
| US-04 | 관리자 | 관리자로서 브라우저를 닫았다 다시 열어도 로그인 상태가 유지되길 원한다, 매번 재로그인하지 않기 위해 | P1 |
| US-05 | 관리자 | 관리자로서 잘못된 비밀번호 입력 시 명확한 에러 메시지를 보고 싶다, 입력 실수를 바로잡기 위해 | P1 |

---

## 3. 기능 명세

| ID | 기능명 | 설명 | 관련 스토리 | 우선순위 |
|----|-------|------|-----------|---------|
| F-01 | 로그인 폼 | 이메일, 비밀번호 입력 필드와 로그인 버튼으로 구성된 폼. 클라이언트 사이드 유효성 검사 포함 | US-01, US-05 | P0 |
| F-02 | 로그인 API | 이메일/비밀번호를 받아 검증 후 JWT 액세스 토큰을 발급하는 API | US-01 | P0 |
| F-03 | 라우트 가드 | 미인증 사용자가 대시보드(`/`) 또는 API에 접근하면 로그인 페이지(`/login`)로 리다이렉트 | US-03 | P0 |
| F-04 | JWT 토큰 관리 | httpOnly 쿠키에 JWT 토큰 저장. 토큰 만료 시 자동 로그아웃 | US-04 | P0 |
| F-05 | 로그아웃 | 토큰 쿠키를 삭제하고 로그인 페이지로 이동 | US-02 | P0 |
| F-06 | 폼 유효성 검사 | 이메일 형식 검증, 비밀번호 빈값 검증. 에러 메시지 실시간 표시 | US-05 | P1 |
| F-07 | 사용자 DB | SQLite 기반 사용자 테이블. bcrypt로 비밀번호 해싱 저장 | US-01 | P0 |
| F-08 | 초기 계정 시딩 | 환경변수에서 초기 관리자 이메일/비밀번호를 읽어 DB에 시딩하는 스크립트 | US-01 | P0 |
| F-09 | 기존 API 보호 | pipeline 관련 기존 API 라우트에 인증 미들웨어를 적용하여 미인증 요청을 차단 | US-03 | P0 |
| F-10 | 로그인 상태 표시 | 대시보드 헤더에 로그인한 사용자 이메일과 로그아웃 버튼 표시 | US-02 | P1 |

---

## 4. 화면 목록

| ID | 화면명 | 경로 | 주요 요소 | 관련 기능 |
|----|-------|------|----------|----------|
| S-01 | 로그인 페이지 | /login | 로고/앱 타이틀, 이메일 입력, 비밀번호 입력, 로그인 버튼, 에러 메시지 영역 | F-01, F-06 |
| S-02 | 대시보드 (수정) | / | 기존 대시보드 + 헤더에 사용자 정보/로그아웃 버튼 추가 | F-10, F-05 |

---

## 5. API 초안

| Method | Path | 설명 | 인증 여부 | 관련 기능 |
|--------|------|------|----------|----------|
| POST | /api/auth/login | 이메일/비밀번호로 로그인, JWT 토큰을 httpOnly 쿠키에 설정 | N | F-02 |
| POST | /api/auth/logout | 쿠키의 JWT 토큰을 삭제하여 로그아웃 처리 | Y | F-05 |
| GET | /api/auth/me | 현재 로그인한 사용자 정보를 반환 (토큰 유효성 검증 포함) | Y | F-04, F-10 |
| POST | /api/pipeline/start | (기존) 파이프라인 시작 — 인증 미들웨어 추가 | Y | F-09 |
| GET | /api/pipeline/list | (기존) 파이프라인 목록 — 인증 미들웨어 추가 | Y | F-09 |
| GET | /api/pipeline/[id] | (기존) 파이프라인 상세 — 인증 미들웨어 추가 | Y | F-09 |
| POST | /api/pipeline/[id]/approve | (기존) 체크포인트 승인 — 인증 미들웨어 추가 | Y | F-09 |
| GET | /api/pipeline/[id]/stream | (기존) SSE 스트림 — 인증 미들웨어 추가 | Y | F-09 |
| GET | /api/pipeline/[id]/outputs/[file] | (기존) 산출물 조회 — 인증 미들웨어 추가 | Y | F-09 |

---

## 6. 엣지케이스 & 예외 처리

| ID | 상황 | 예상 동작 | 관련 기능 |
|----|------|----------|----------|
| E-01 | 존재하지 않는 이메일로 로그인 시도 | "이메일 또는 비밀번호가 올바르지 않습니다" 에러 표시 (이메일 존재 여부를 노출하지 않음) | F-02 |
| E-02 | 잘못된 비밀번호로 로그인 시도 | E-01과 동일한 에러 메시지 표시 (타이밍 공격 방지를 위해 동일 응답 시간) | F-02 |
| E-03 | JWT 토큰 만료 후 API 요청 | 401 응답 반환, FE에서 감지하여 로그인 페이지로 리다이렉트 | F-04, F-03 |
| E-04 | 이미 로그인한 사용자가 /login 접근 | 대시보드(`/`)로 리다이렉트 | F-03 |
| E-05 | 빈 이메일 또는 비밀번호로 제출 | 클라이언트 사이드에서 제출 차단, 인라인 에러 메시지 표시 | F-06 |
| E-06 | 잘못된 형식의 이메일 입력 | 클라이언트 사이드에서 이메일 형식 검증 실패 메시지 표시 | F-06 |
| E-07 | JWT 시크릿 환경변수 미설정 | 서버 시작 시 에러 로그 출력 및 기본값 사용 방지 (앱 시작 실패) | F-04 |
| E-08 | 토큰이 변조된 요청 | 401 응답 반환, 쿠키 삭제 | F-04 |
| E-09 | DB 파일 손상 또는 접근 불가 | 500 에러 반환, 서버 로그에 상세 에러 기록 | F-07 |
| E-10 | SSE 스트림 연결 중 토큰 만료 | 스트림 연결 종료, 클라이언트에서 재인증 요청 | F-09, F-03 |

---

## 7. 비기능 요구사항

| 항목 | 기준 | 비고 |
|------|------|------|
| 성능 | 로그인 API 응답 500ms 이내 | bcrypt 해싱 비용(cost factor 10~12)을 고려한 기준 |
| 보안 | 비밀번호는 bcrypt로 해싱하여 저장 | 평문 저장 금지. salt 자동 생성 |
| 보안 | JWT 토큰은 httpOnly + Secure + SameSite=Strict 쿠키에 저장 | XSS로 토큰 탈취 방지 |
| 보안 | JWT 만료 시간 24시간 | 만료 후 재로그인 필요 |
| 보안 | 로그인 실패 시 이메일 존재 여부 미노출 | 열거 공격(Enumeration Attack) 방지 |
| 보안 | CSRF 방지를 위한 SameSite 쿠키 설정 | Next.js 동일 출처이므로 SameSite=Strict로 충분 |
| 접근성 | 로그인 폼에 적절한 label, aria 속성 적용 | 스크린 리더 대응 |
| 접근성 | 키보드만으로 로그인 가능 (Tab 이동, Enter 제출) | 마우스 없이 조작 가능 |
| 접근성 | 에러 메시지를 aria-live 영역에 표시 | 보조 기술에 에러 즉시 전달 |
| UX | 기존 다크 테마(#0a0a14 배경)와 일관된 디자인 | 기존 CSS 변수 활용 |
| UX | 로그인 중 로딩 상태 표시 (버튼 스피너) | 중복 제출 방지 겸용 |
| 인프라 | SQLite DB 파일은 프로젝트 루트의 data/ 디렉토리에 저장 | Docker 볼륨 마운트 고려 |
| 인프라 | 환경변수: JWT_SECRET, ADMIN_EMAIL, ADMIN_PASSWORD | .env 파일로 관리, .gitignore에 추가 |
